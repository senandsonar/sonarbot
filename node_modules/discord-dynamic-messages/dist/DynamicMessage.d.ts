import { DMChannel, Message, MessageEmbed, TextChannel, User, NewsChannel } from 'discord.js';
import { IDynamicMessageConfig } from './interfaces/IDynamicMessageConfig';
export declare abstract class DynamicMessage {
    set message(newMessage: Message);
    get message(): Message;
    private isResponse;
    private responseTo;
    private metadata;
    private config;
    private reactionCollector;
    private __message;
    private emojiFixes;
    constructor(config?: IDynamicMessageConfig);
    sendTo(channel: TextChannel | DMChannel | NewsChannel): Promise<this>;
    replyTo(msg: Message): Promise<this>;
    attachTo(message: Message, responseTo?: User): this;
    reRender(): void;
    addReactions(emojiNames: string[]): Promise<any>;
    protected abstract render(): string | MessageEmbed;
    private tearDownReactionCollector;
    private setupReactionCollector;
    private handleReactionRemoved;
    private handleReaction;
}
