"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const manageMetadata_1 = require("../manageMetadata");
const defaultReactionConfig = () => ({
    triggerRender: true,
    ignoreBots: true,
    ignoreHumans: false,
});
exports.OnAnyReactionRemoved = (config = {}) => (target, propertyKey, descriptor) => {
    manageMetadata_1.metadata.update(target, (allMetadata) => {
        // Add a catch all reaction handler
        allMetadata.catchAllReactionRemovedHandler = {
            handlerKey: propertyKey,
            config: Object.assign(Object.assign({}, defaultReactionConfig()), config),
        };
        return allMetadata;
    });
};
